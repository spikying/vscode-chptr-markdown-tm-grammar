fileTypes: [md, chptr]
injectionSelector: "meta.paragraph.markdown"
patterns:
  chptr-markup:
    # captures:
    #   '1': {name: punctuation.definition.raw.markdown}
    #   '2': {name: markup.bold.markdown} # {name: heading.6.markdown}
    #   '3': {name: punctuation.definition.raw.markdown}
    # match: ({)([^:,.!\n{}]+?)(})
    # name: markup.italic.markdown
    name: "chptr-markup"
    begin: ({)
    end: (})
    contentName: markup.italic.markdown
  # chptr-kv-markup:
  #   captures:
  #     '1': {name: punctuation.definition.raw.markdown}
  #     '2': {name: string.other.link.description.title.markdown}
  #     '3': {name: comment.block.html}
  #     '4': {name: punctuation.definition.raw.markdown}
  #   match: ({)([^}]*?\s?):(\s?.*?)(})
  #   name: markup.italic.markdown
  # chptr-para-markup:
  #   captures:
  #     '1': {name: punctuation.definition.raw.markdown}
  #     '2': {name: markup.italic.markdown}
  #     '3': {name: punctuation.definition.raw.markdown}
  #   match: ({{)(\d+?)(}})
  #   name: comment.block.html
scopeName: markdown.chptr
